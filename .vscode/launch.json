{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Launch observer",
            "type": "go",
            "request": "launch",
            "mode": "debug",
            "program": "${workspaceFolder}/observer",
            "cwd": "${workspaceFolder}"
        }
    ]
}
